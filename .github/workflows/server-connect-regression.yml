name: Verify connection lifecycle regression
on:
  push:
    branches: [work/server-connect-validation-20260923]
    paths: [.github/workflows/server-connect-regression.yml]
permissions:
  contents: read
jobs:
  import:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 1fee3de71e61716a361534c6bae9151ee217be5c
          fetch-depth: 0
          lfs: false
      - name: Reconstruct exact reviewed changes without updating product branches
        shell: bash
        run: |
          python3 - <<'PY'
          import json,pathlib,subprocess
          def git(*args,data=None): return subprocess.check_output(['git',*args],input=data)
          parent='1fee3de71e61716a361534c6bae9151ee217be5c'
          assert git('rev-parse','HEAD').decode().strip()==parent
          changes=json.loads(r'''[{"path":"docs/implementation/server-connection.md","old":"da2608fd1434a58bb4389ddad06f7e7bf0340fd2","new":"786701d2b43c550e2120b3d963a890a2d91c2df9","ops":[[103,103,"\n## Connection lifecycle regression hardening\n\nFault-injection and real loopback HTTP/WebSocket regressions reproduced boundary\nfailures that were not covered by the initial happy-path suite. This follow-up:\n\n- Keeps discovery and HTTP deadlines active even when a caller supplies its own\n  cancellation signal; the deadline also bounds response-body consumption.\n- Requires encrypted storage acknowledgement before opening browser authorization\n  with a new device key, including a retry after an earlier storage failure.\n- Fences protected requests until newly issued credentials are saved. A failed\n  write retains the received credentials only for a storage retry in this process,\n  rather than replaying an exchange with the predecessor refresh token. Concurrent\n  callers await one write. A process exit during failed storage still requires\n  normal session recovery; this does not promise crash-proof token rotation.\n- Drains obsolete connection attempts before reconnecting in a new generation.\n  Stale completions cannot acknowledge a new attempt or schedule its recovery.\n- Rejects malformed event tickets/challenges before creating a WebSocket and\n  contains signing/send errors within the connection error path.\n- Lets cancellation/authorization timeout close the callback listener even if OS\n  browser launch has not settled. Cancelling a pending login releases only that\n  local attempt; a subsequent login still needs server authorization.\n\nNo server approval policy, DPoP verification, resource permissions, task freeze,\nchat scheduling, client installation flow or renderer UI is replaced. The new\nregressions supplement, rather than weaken, the original enrollment and runtime\nchecks. Exact-source macOS/LFS, real Host/Shell, security-freeze and native UI\nacceptance remain distinct from controlled HTTP/browser fixtures.\n"]]},{"path":"source/client-connections/discovery.ts","old":"bfee70de0efd0ba07b61b509e252f317acfb5856","new":"23e0f2a70262fe7a2710b824f37653e8270399d8","ops":[[40,41,"export async function discoverNode(address: string, cancellation?: AbortSignal): Promise<DiscoveredNode> {\n  // Caller cancellation must not remove the shared deadline for both documents.\n  const deadline = AbortSignal.timeout(15_000);\n  const signal = cancellation ? AbortSignal.any([cancellation, deadline]) : deadline;\n"]]},{"path":"source/client-connections/manager.ts","old":"9385483d02c3c77cb62516e83dc39ee920482941","new":"f95e97b524d3f34ac41741b7bcedf8bd9e6e3c3c","ops":[[24,24,"  connectingGeneration?: number;\n  credentialsDirty?: boolean;\n  savingCredentials?: Promise<void>;\n"],[71,71,"  }\n\n  /** Do not use newly issued credentials until encrypted storage acknowledges\n   * them. Preserve a received rotated token in memory for a storage-only retry;\n   * never repeat the network exchange with its already-consumed predecessor. */\n  private async persistCredentials(c: Connection): Promise<void> {\n    while (c.credentialsDirty) {\n      if (c.savingCredentials) { await c.savingCredentials; continue; }\n      const key = c.deviceKeyPem, refresh = c.refreshToken;\n      const operation = this.save();\n      c.savingCredentials = operation;\n      try {\n        await operation;\n        if (c.deviceKeyPem === key && c.refreshToken === refresh) c.credentialsDirty = false;\n      } finally { if (c.savingCredentials === operation) delete c.savingCredentials; }\n    }\n  }\n\n  private isCurrent(c: Connection, generation: number): boolean {\n    return !this.closed && c.generation === generation && this.connections.get(c.profile.id) === c;\n"],[142,143,"        c.credentialsDirty = true;\n"],[144,144,"      if (abort.signal.aborted || c.generation !== generation) throw new Error(\"Sign-in cancelled.\");\n      // Retry a failed key write even when the generated key is already in memory.\n      await this.persistCredentials(c);\n"],[162,163,"    c.credentialsDirty = true;\n    await this.persistCredentials(c);\n"],[166,166,"    const generation = c.generation;\n    const assertCurrent = () => { if (!this.isCurrent(c, generation)) throw new Error(\"The server session changed during sign-in.\"); };\n    await this.persistCredentials(c); assertCurrent();\n"],[170,171,""],[172,173,"      await this.verifyIdentity(c); assertCurrent();\n"],[209,210,"    const generation = c.generation;\n    if (!c.socket && !c.timer && c.refreshToken) void this.connect(c).catch(error => { if (this.isCurrent(c, generation)) this.scheduleReconnect(c, error); });\n"],[228,229,"    const generation = c.generation;\n    try { await this.connect(c); } catch (error) { if (this.isCurrent(c, generation)) this.scheduleReconnect(c, error); throw error; }\n"],[232,233,"    const generation = c.generation;\n    if (c.connecting) {\n      const previous = c.connecting;\n      if (c.connectingGeneration === generation) return previous;\n      // Drain obsolete work before a fresh connection. Do not create competing\n      // refresh exchanges, and do not mistake an old completion for this attempt.\n      await previous.catch(() => {});\n      if (!this.isCurrent(c, generation)) return;\n      return this.connect(c);\n    }\n"],[234,236,"    c.connecting = operation; c.connectingGeneration = generation;\n    try { await operation; }\n    finally { if (c.connecting === operation) { delete c.connecting; delete c.connectingGeneration; } }\n"],[243,243,"    if (!this.isCurrent(c, generation)) return;\n"],[244,244,"    if (!this.isCurrent(c, generation)) return;\n"],[245,246,"    if (typeof ticket !== \"string\" || !ticket || ticket.length > 4096 || typeof nonce !== \"string\" || !/^[A-Za-z0-9_-]{16,128}$/.test(nonce)) throw new Error(\"Invalid event session challenge.\");\n"],[254,255,"      try { socket.send(JSON.stringify({ ticket, proof: c.device!.eventProof(ticket, nonce), after: c.snapshot?.cursor ?? 0 })); }\n      catch (error) { c.profile.error = this.message(error); socket.terminate(); }\n"],[293,294,"      void this.connect(c).catch(next => { if (this.isCurrent(c, generation)) this.scheduleReconnect(c, next); });\n"],[363,364,"    c.profile.status = \"signed-out\"; c.credentialsDirty = true; await this.persistCredentials(c); this.changed(c);\n"],[379,380,"    c.generation++;\n    const login = c.login; delete c.login; login?.abort();\n    delete c.profile.loginStage;\n"]]},{"path":"source/client-connections/transport.ts","old":"437a88811d5131eb73d1eaeb08fd39d31b977f3c","new":"244e1bc7822eed18712482fcebb5e36911f60833","ops":[[23,23,"  // Cancellation and the request deadline are independent; keep both through\n  // response-body consumption, including token exchange during browser login.\n  const deadline = AbortSignal.timeout(20_000);\n  const signal = init.signal ? AbortSignal.any([init.signal, deadline]) : deadline;\n"],[25,26,"    ...init, redirect: \"error\", signal,\n"],[122,123,"    // A slow OS browser launch must not hold the callback listener open after\n    // cancellation or the authorization deadline. A real callback can also win.\n    await Promise.race([openExternal(url.href), receivedCode]);\n"]]},{"path":"tests/node-client.test.mjs","old":"3a08b39ac6961d066e43ed495d6f7d2fdca97fb1","new":"912cc9036f785c9c9a54bc261df9b036e765d4c8","ops":[[34,35,"  let eventMode = \"ready\", lastSocket, eventNonce = \"test-event-nonce-123456\", nextIdentityGate;\n"],[41,41,"    if (req.url === \"/v1/node\" && nextIdentityGate) { const gate = nextIdentityGate; nextIdentityGate = undefined; gate.started(); await gate.wait; }\n"],[57,58,"    if (req.url === \"/v1/events/ticket\") return json({ ticket: \"single-use-ticket\", nonce: eventNonce });\n"],[92,92,"    setEventNonce(value) { eventNonce = value; },\n    holdNextIdentity(gate) { nextIdentityGate = gate; },\n"],[219,219,"\n\nfunction deferred() {\n  let resolve; const promise = new Promise(done => { resolve = done; });\n  return { promise, resolve };\n}\n\ntest(\"failed device-key persistence cannot be bypassed by retrying browser login\", async () => {\n  const node = await fakeNode(); let failKey = false, saved = [], opened = 0;\n  const manager = new NodeConnectionManager({\n    async load() { return []; },\n    async save(records) {\n      if (failKey && records.some(r => r.deviceKeyPem)) throw new Error(\"Test keychain locked\");\n      saved = structuredClone(records);\n    },\n  }, async url => { opened++; await node.open(url); });\n  try {\n    const p = await manager.add(node.baseUrl); failKey = true;\n    await assert.rejects(manager.login(p.id), /keychain locked/);\n    await assert.rejects(manager.login(p.id), /keychain locked/);\n    assert.equal(opened, 0, \"no browser approval with an unsaved device key\");\n    assert.equal(saved[0].deviceKeyPem, undefined);\n    failKey = false; await manager.login(p.id);\n    assert.equal(opened, 1); assert.ok(saved[0].deviceKeyPem); assert.ok(saved[0].refreshToken);\n  } finally { manager.close(); await node.close(); }\n});\n\ntest(\"failed token persistence fences protected requests until a durable retry succeeds\", async () => {\n  const node = await fakeNode(); let failTokens = true, saved = [];\n  const manager = new NodeConnectionManager({\n    async load() { return []; },\n    async save(records) {\n      if (failTokens && records.some(r => r.refreshToken)) throw new Error(\"Test token storage unavailable\");\n      saved = structuredClone(records);\n    },\n  }, url => node.open(url));\n  try {\n    const p = await manager.add(node.baseUrl);\n    await assert.rejects(manager.login(p.id), /storage unavailable/);\n    const before = node.requests.length;\n    await assert.rejects(manager.submitGoal(p.id, { botId: \"bot\", prompt: \"Do not send until saved\" }, \"durable-session-request\"), /storage unavailable/);\n    assert.equal(node.requests.length, before, \"no protected traffic while credentials are not durable\");\n    assert.equal(node.goals.length, 0); assert.equal(saved[0].refreshToken, undefined);\n    failTokens = false;\n    await manager.submitGoal(p.id, { botId: \"bot\", prompt: \"Do not send until saved\" }, \"durable-session-request\");\n    assert.equal(node.goals.length, 1); assert.equal(saved[0].refreshToken, node.tokens.refresh_token);\n    assert.equal(node.refreshCount, 0, \"persist the received credentials, never blindly rotate again\");\n  } finally { manager.close(); await node.close(); }\n});\n\ntest(\"malformed event nonce is rejected before opening a socket or invoking its callbacks\", async () => {\n  const node = await fakeNode(); node.setEventNonce(\"invalid\");\n  const manager = new NodeConnectionManager({ async load() { return []; }, async save() {} }, url => node.open(url));\n  try {\n    const p = await manager.add(node.baseUrl);\n    await assert.rejects(manager.login(p.id), /event (session|challenge)/i);\n    assert.equal(node.socketCount, 0); assert.notEqual((await manager.list())[0].status, \"online\");\n  } finally { manager.close(); await node.close(); }\n});\n\ntest(\"a new reconnect does not reuse an obsolete in-flight connection attempt\", async () => {\n  const node = await fakeNode();\n  const manager = new NodeConnectionManager({ async load() { return []; }, async save() {} }, url => node.open(url));\n  const started = deferred(), release = deferred();\n  try {\n    const p = await manager.add(node.baseUrl); await manager.login(p.id);\n    await new Promise(r => setTimeout(r, 30));\n    const count = node.socketCount;\n    node.holdNextIdentity({ started: started.resolve, wait: release.promise });\n    const old = manager.resume(p.id); void old.catch(() => {}); await started.promise;\n    const current = manager.resume(p.id); void current.catch(() => {});\n    release.resolve(); await Promise.allSettled([old, current]);\n    for (let n = 0; n < 100 && (await manager.list())[0].status !== \"online\"; n++) await new Promise(r => setTimeout(r, 10));\n    assert.equal((await manager.list())[0].status, \"online\");\n    assert.equal(node.socketCount, count + 1, \"only the current attempt creates a replacement socket\");\n  } finally { release.resolve(); manager.close(); await node.close(); }\n});\n\ntest(\"cancelling while a device key is saving allows a fresh login without duplicate browser grants\", async () => {\n  const node = await fakeNode(), started = deferred(), release = deferred();\n  let hold = true, opened = 0;\n  const manager = new NodeConnectionManager({\n    async load() { return []; },\n    async save(records) {\n      if (hold && records.some(r => r.deviceKeyPem)) { started.resolve(); await release.promise; }\n    },\n  }, async url => { opened++; await node.open(url); });\n  try {\n    const p = await manager.add(node.baseUrl);\n    const first = manager.login(p.id); const cancelled = assert.rejects(first, /cancelled/);\n    await started.promise; await manager.cancelLogin(p.id);\n    const second = manager.login(p.id); void second.catch(() => {});\n    hold = false; release.resolve();\n    await cancelled; await second;\n    assert.equal(opened, 1);\n    assert.equal(node.requests.filter(r => r.path === \"/oauth/token\").length, 1);\n    assert.equal(node.requests.filter(r => r.path === \"/oauth/revoke\").length, 0);\n  } finally { release.resolve(); manager.close(); await node.close(); }\n});\n\ntest(\"concurrent protected requests wait for one durable credential retry\", async () => {\n  const node = await fakeNode(), started = deferred(), release = deferred();\n  let failure = true, heldWrites = 0;\n  const manager = new NodeConnectionManager({\n    async load() { return []; },\n    async save(records) {\n      if (!records.some(r => r.refreshToken)) return;\n      if (failure) throw new Error(\"Test credential write failure\");\n      heldWrites++; started.resolve(); await release.promise;\n    },\n  }, url => node.open(url));\n  try {\n    const p = await manager.add(node.baseUrl);\n    await assert.rejects(manager.login(p.id), /write failure/); failure = false;\n    const send = () => manager.submitGoal(p.id, { botId: \"bot\", prompt: \"One durable task\" }, \"same-task-key\");\n    const first = send(), second = send(); void first.catch(() => {}); void second.catch(() => {});\n    await started.promise; await new Promise(r => setTimeout(r, 20));\n    assert.equal(heldWrites, 1); assert.equal(node.goals.length, 0);\n    release.resolve(); await Promise.all([first, second]);\n    assert.equal(heldWrites, 1); assert.equal(node.goals.length, 1);\n    assert.equal(node.requests.filter(r => r.path === \"/oauth/token\").length, 1);\n  } finally { release.resolve(); manager.close(); await node.close(); }\n});\n\ntest(\"local cancellation closes its callback listener even while browser launch is pending\", async () => {\n  const node = await fakeNode(), launched = deferred(), finishLaunch = deferred(), abort = new AbortController();\n  let callback;\n  const authorization = authorizeNode(node.baseUrl, async url => {\n    callback = new URL(url).searchParams.get(\"redirect_uri\"); launched.resolve(); await finishLaunch.promise;\n  }, new DpopClient(node.baseUrl, generateDpopKey()), abort.signal);\n  const result = authorization.then(() => \"unexpected success\", error => error.message);\n  try {\n    await launched.promise; abort.abort();\n    const state = await Promise.race([result, new Promise(r => setTimeout(() => r(\"still waiting for browser launch\"), 150))]);\n    assert.match(state, /cancelled/);\n    await assert.rejects(fetch(callback), /fetch failed/);\n    assert.equal(node.requests.filter(r => r.path === \"/oauth/token\").length, 0);\n  } finally { finishLaunch.resolve(); await result; await node.close(); }\n});\n"]]},{"path":"tests/node-connection-discovery.test.mjs","old":"7fe54648fce05d1c8ff4b7e8881d3ac0a0429189","new":"8ab090c7fbcbb099dd274d05f89ef794f01eaf53","ops":[[12,14,"await build({stdin:{contents:`export * from './source/client-connections/discovery.ts'; export * from './source/client-connections/manager.ts'; export * from './source/client-connections/transport.ts';`,resolveDir:root}, outfile:path.join(temp,\"test.mjs\"),bundle:true,platform:\"node\",format:\"esm\",banner:{js:'import{createRequire}from\"node:module\";const require=createRequire(import.meta.url);'},logLevel:\"silent\"});\nconst { validateNodeDiscovery, discoverNode, fetchNodeJson, NodeConnectionManager } = await import(pathToFileURL(path.join(temp,\"test.mjs\")));\n"],[88,88,"\n\nfor (const [name, run] of [\n  [\"discovery\", signal => discoverNode(base, signal)],\n  [\"token transport\", signal => fetchNodeJson(base, \"/oauth/token\", { signal, method: \"POST\" })],\n]) test(`${name} retains its deadline when the caller supplies cancellation`, async t => {\n  const caller = new AbortController(), expired = new Error(\"Test request deadline\");\n  t.mock.method(AbortSignal, \"timeout\", () => AbortSignal.abort(expired));\n  t.mock.method(globalThis, \"fetch\", async (url, init) => {\n    init.signal.throwIfAborted();\n    return new Response(JSON.stringify(String(url).endsWith(\"/v1/node\") ? node() : metadata(base)));\n  });\n  await assert.rejects(run(caller.signal), error => error === expired);\n  assert.equal(caller.signal.aborted, false, \"the request must not mutate caller cancellation\");\n});\n\ntest(\"bounded discovery also honours explicit cancellation without starting a request\", async t => {\n  const cancelled = new Error(\"Test caller cancellation\");\n  t.mock.method(globalThis, \"fetch\", async (_url, init) => { init.signal.throwIfAborted(); assert.fail(\"must abort before network\"); });\n  await assert.rejects(discoverNode(base, AbortSignal.abort(cancelled)), error => error === cancelled);\n});\n"]]}]''')
          for change in changes:
              path=pathlib.Path(change['path'])
              assert str(path).startswith(('source/client-connections/','tests/','docs/implementation/'))
              assert not path.is_absolute() and '..' not in path.parts and not path.is_symlink()
              assert git('hash-object',str(path)).decode().strip()==change['old'],str(path)
              lines=path.read_text().splitlines(keepends=True)
              for start,end,replacement in reversed(change['ops']):
                  assert 0<=start<=end<=len(lines)
                  lines[start:end]=replacement.splitlines(keepends=True)
              path.write_text(''.join(lines))
              assert git('hash-object',str(path)).decode().strip()==change['new'],str(path)
              git('add',str(path))
          tree=git('write-tree').decode().strip()
          assert tree=='8ddcd68c7e690be79e25b4bc237b41bc28ba9c10'
          commit='tree '+tree+'\nparent '+parent+'\nauthor BeeBot development <151381126+yongchaoyin@users.noreply.github.com> 1790156814 +0000\ncommitter BeeBot development <151381126+yongchaoyin@users.noreply.github.com> 1790156814 +0000\n\nfix(connections): fence credentials and obsolete connection lifecycles\n\nPreserve cancellation deadlines, durable device-key and token storage, generation-aware reconnects, bounded event challenges and browser callback cleanup. Add ten regression cases including seven pre-fix failures; retain all original authorization, scope, UI and runtime assertions.\n'
          sha=git('hash-object','-t','commit','-w','--stdin',data=commit.encode()).decode().strip()
          assert sha=='d6dd284822cf78afd15736221da141635e7496f5'
          git('update-ref','HEAD',sha,parent)
          assert not git('status','--porcelain').strip()
          git('push','origin','HEAD:refs/heads/work/server-connect-regression-candidate-20260923')
          print('Verified source:',sha,'tree:',tree)
          PY
  verify:
    needs: import
    runs-on: macos-15
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: d6dd284822cf78afd15736221da141635e7496f5
          lfs: true
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 26.5.0
          cache: npm
      - name: Record exact source and environment
        run: |
          test "$(git rev-parse HEAD)" = d6dd284822cf78afd15736221da141635e7496f5
          test "$(git rev-parse HEAD^{tree})" = 8ddcd68c7e690be79e25b4bc237b41bc28ba9c10
          mkdir -p /tmp/connection-regression
          git show -s --format=fuller HEAD > /tmp/connection-regression/commit.txt
          git rev-parse HEAD^{tree} > /tmp/connection-regression/tree.txt
          node --version > /tmp/connection-regression/node.txt
          sw_vers > /tmp/connection-regression/os.txt
      - run: npm ci --no-audit --no-fund
      - run: npm run bootstrap
      - run: npm run icon:generate
      - name: Full source and regression suite
        shell: bash
        run: |
          set -o pipefail
          npm run check 2>&1 | tee /tmp/connection-regression/check.log
      - name: Frontend build
        shell: bash
        run: |
          set -o pipefail
          npm run frontend:build 2>&1 | tee /tmp/connection-regression/frontend.log
      - name: Lossless publication export
        shell: bash
        run: |
          set -o pipefail
          npm run publication:check 2>&1 | tee /tmp/connection-regression/publication.log
      - name: Real Host and Shell with deterministic model
        shell: bash
        run: |
          set -o pipefail
          npm run node:test:integration 2>&1 | tee /tmp/connection-regression/runtime.log
      - name: Real device task freeze
        shell: bash
        run: |
          set -o pipefail
          BEEBOT_RUNTIME_INTEGRATION=1 node --test tests/node-task-security-runtime.test.mjs 2>&1 | tee /tmp/connection-regression/task-security.log
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: connection-regression-verification
          path: /tmp/connection-regression/
          retention-days: 7
